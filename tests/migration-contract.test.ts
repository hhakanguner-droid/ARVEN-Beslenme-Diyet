import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import test from "node:test";
const initial=readFileSync("db/migrations/0001_initial.sql","utf8");
test("clean V1 migration keeps persistence small and structurally explicit",()=>{assert.match(initial,/CREATE TABLE users/);assert.match(initial,/subject TEXT PRIMARY KEY NOT NULL/);assert.match(initial,/CREATE TABLE goal_versions/);assert.match(initial,/reference_snapshots_json/);assert.match(initial,/CREATE TABLE food_versions/);assert.match(initial,/CREATE TABLE portion_versions/);assert.match(initial,/CREATE TABLE ai_action_proposals/);assert.match(initial,/CREATE TABLE ai_action_decisions/);assert.match(initial,/CREATE TABLE ai_action_outcomes/);assert.match(initial,/CREATE TABLE nutrition_events/);assert.match(initial,/outcome IN \('applied','failed'\)/);assert.match(initial,/mifflin-st-jeor@v1/);assert.match(initial,/MealLogActionV1/);assert.match(initial,/WaterLogActionV1/);assert.match(initial,/STRICT, WITHOUT ROWID/);assert.doesNotMatch(initial,/CREATE TRIGGER/i);assert.doesNotMatch(initial,/CREATE TABLE ai_actions\b/);assert.doesNotMatch(initial,/meal_entry_items/);assert.doesNotMatch(initial,/food_nutrients/);assert.doesNotMatch(initial,/user_medications/);});
